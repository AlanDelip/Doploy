version: "3.7"

services:
  web:
    build: .
    ports:
      - 5000:5000
    volumes:
      - ./app/:/home/app/
    depends_on:
      - db

  db:
    image: postgres:10
    env_file: .env
    volumes:
      - pg-data:/var/lib/postgresql/data
    expose:
      - 5432

volumes:
  pg-data: